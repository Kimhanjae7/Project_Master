<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/footer.css">
    
</head>
<body>
    <h1>홈페이지 시작입니다</h1>
    <header>
        <nav>
            <button data-href="/">홈</a>  |
            <button data-href="/profile">프로필</a>   |
            <button data-href="board">게시판</button>
            <% if(user_id){ %>
                <%=user_id%>님
                <button data-href="/logout">로그아웃</a>
                <button data-href="/registration">게시글 등록</a>
            <% } else{ %>
                <button data-href="/login">로그인</a>   |
                <button data-href="/join">회원가입</a>   |
            <% } %>
        </nav>
    </header>
<script>
    document.querySelectorAll('nav button').forEach(button => {
    button.addEventListener('click', function() {
        // 클릭한 버튼의 href 속성값을 가져와서 해당 링크로 이동
        window.location.href = this.getAttribute('data-href');
    });
});
</script>
